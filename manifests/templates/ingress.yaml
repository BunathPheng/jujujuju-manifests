---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jujujuju                  # Ingress name
  labels:
    app: jujujuju
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "jujujuju.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jujujuju               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
